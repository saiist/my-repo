name: Hello GitHub Workflows

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "Hello, GitHub Workflows!"
    - uses: actions/checkout@v4